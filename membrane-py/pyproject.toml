[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "membrane-py"
version = "0.1.0"
description = "Python client library for Membrane Proxy and Swarm Extraction Engine"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "openai>=1.0.0",
    "requests>=2.25.0"
]
