Membrane Guard: Master Production Release Plan
Repository Strategy: Single Monolithic Repository (membrane-guard)
Licensing Core: Elastic License 2.0 (ELv2)
This document serves as the complete, production-ready Master Release Plan for the engineering team. It unifies our security posture, fixes all 21 systemic vulnerabilities exposed in the white-box QA audit, and integrates our high-fidelity data-mining architecture directly into the core framework.
⚙️ Global Environment Configuration & Constants
All engineering tracks must standardize their local and cloud deployments using the following environment definitions:
Plaintext
POLAR_ORGANIZATION_TOKEN=polar_oat_SCucYGgUjzwMS9PSP47d8oDPDM1JKE8d7BoR12Eq6Bn
MEMBRANE_FLASH_MODEL=gemini/gemini-2.5-flash
MEMBRANE_APEX_MODEL=gemini/gemini-3.5-flash
DATABASE_SSL=false
MAX_SANDBOX_CHUNKS=25
🗺️ Master Release Sequence
 ┌──────────────────────────────────────┐
 │ Sprint 1: Security, Economics, &     │
 │ Legal Licensing Perimeter            │
 └──────────────────┬───────────────────┘
                    │
                    ▼
 ┌──────────────────────────────────────┐
 │ Sprint 2: Data Isolation, Caching,   │
 │ & Database Architecture Hardening    │
 └──────────────────┬───────────────────┘
                    │
                    ▼
 ┌──────────────────────────────────────┐
 │ Sprint 3: High-Fidelity Interactive  │
 │ Playground & Developer UX Overhaul   │
 └──────────────────────────────────────┘
🛠️ Sprint 1: Security, Economics, & Legal Licensing Perimeter
Primary Focus: Eliminate immediate cash leakage, fix the safety firewall race condition, deploy the Elastic License, and build the generalized, tier-gated Map-Reduce engine core.
📋 Task Breakdown & Code Modifications
	•	NEW-01 (Repository Root) — Deploy the Repo Legal Shield: Add an official LICENSE file containing the full text of the Elastic License 2.0 (ELv2). This permits full developer code inspection while legally blocking competitors from cloning and reselling Membrane as a standalone hosted proxy utility.
	•	QA-10 / QA-21 (server.py) — Hard-Wire Polar.sh Authentication: Strip out all legacy mock license verification routines. Implement an asynchronous boot-time network handshake that calls Polar's official server-side endpoint (POST /v1/customer-portal/license-keys/validate). If the verification succeeds, set a global flag: license_active = True.
	•	NEW-02 (server.py) — Deploy the Production Fail-Open Guard: Wrap the Polar.sh outbound network check in a structured try/except block. If Polar's authentication servers experience an unexpected network timeout or error, log a critical warning and default to license_active = True to prevent customer live production apps from dropping offline.
	•	NEW-03 (server.py) — Generalize the /v1/swarm/map Input Core: Refactor the endpoint handler to accept an agnostic, multi-workload execution payload supporting dynamic text arrays, arbitrary strings, target keyword arrays, and custom system persona scopes rather than hardcoded parsing rules. This enables the same endpoint to process legal contracts, speech transcripts, or zoning files identically.
	•	NEW-04 (server.py) — Build the Dual-Path Scale Gate: Implement an engineering throttle based on the active license state:
	◦	Free Sandbox Path: If license_active == False, enforce a strict local scale cap of 25 concurrent text chunks. If a request exceeds 25 chunks, truncate the array slice cleanly, log an upgrade notification, and run a standard concurrent loop.
	◦	Enterprise Path: If license_active == True, remove the sandbox throttle completely and activate our sliding-window queue manager to prevent cloud provider HTTP 429 rate limits on large files.
	•	QA-01 (server.py) — Fix Multi-Agent Key Collision: Locate the auto-provisioning step inside verify_access. Strip out the hardcoded "local_dev" tenant string and replace it with a dynamically generated identifier derived from a slice of the incoming API key hash (tenant_id = f"local_dev_{hashed_key[:8]}") to satisfy unique table schema criteria.
	•	QA-02 (server.py) — Enforce the Absolute WAF Firewall Gate: Eliminate the parallel asyncio.FIRST_COMPLETED race vulnerability. Ensure the server never returns data to the client until the semantic safety bouncer task has fully completed execution and verified the prompt is safe.
	•	QA-03 (server.py) — Decouple the Offline Boot Lifespan: Modify the FastAPI startup lifespan hook. Demote the strict GEMINI_API_KEY runtime assertion to a passive logging warning check so the proxy container can boot cleanly in air-gapped environments utilizing local models.
	•	QA-19 (server.py) — Secure Token Ledgers Against Schema Failures: Rewrite the Map-Reduce exception handling matrices. Ensure token consumption parameters are parsed and ledger records are written immediately following the upstream network call, regardless of whether subsequent local JSON string parsing fails.
	•	QA-15 (server.py) — Unbind PII Scrubbing from License Validation: Move the scrub_pii parsing chain outside of the paid license validation check. Ensure user payloads are aggressively scrubbed for sensitive data across all free testing profiles to protect local database tables.
	•	QA-06 / QA-07 (server.py) — Implement Value-Based Accounting Code: Completely strip out the randomized random.uniform(1.7, 2.3) float markup logic. Introduce a pricing tier that logs local/Ollama models at a clean $0.00 wholesale rate. Wire up the new running cost-savings tracker module.
🔩 Core Code Implementation Requirements (Sprint 1)
The Decoupled 25-Chunk Scale Gate Block
Python
# Insert directly inside the POST /v1/swarm/map router core
MAX_SANDBOX_CHUNKS = 25
 
if not global_state.license_active:
    if len(request.chunks) > MAX_SANDBOX_CHUNKS:
        active_chunks = request.chunks[:MAX_SANDBOX_CHUNKS]
        print(f"⚠️ Scale Throttle: Capping unvalidated run at {MAX_SANDBOX_CHUNKS} chunks.")
    else:
        active_chunks = request.chunks
    
    return await execute_basic_sandbox_gather(active_chunks)
else:
    return await execute_sliding_window_queue(request.chunks, worker_pool)
Value-Based Token Accounting Ledger Math
Python
def calculate_token_savings(model_name: str, raw_tokens: int, optimized_tokens: int):
    # Static industry baseline rates per 1M tokens
    if "flash" in model_name.lower():
        input_rate = 0.075
        output_rate = 0.30
    else:
        input_rate = 1.25
        output_rate = 5.00
        
    actual_cost = (optimized_tokens / 1000000) * input_rate
    hypothetical_cost = (raw_tokens / 1000000) * input_rate
    net_savings = max(0.0, hypothetical_cost - actual_cost)
    
    return {
        "actual_cost_incurred": actual_cost,
        "gross_unoptimized_cost": hypothetical_cost,
        "net_enterprise_savings": net_savings
    }
🔐 Sprint 2: Data Isolation, Caching, & Database Hardening
Primary Focus: Prevent cross-tenant data leakage, eliminate transaction lockups, and construct physical database tables for telemetry tracking.
📋 Task Breakdown & Code Modifications
	•	QA-11 (Next.js Frontend — src/app/dashboard/): Erase Inline Dynamic DDL Queries: Completely delete the five inline ALTER TABLE ... ADD COLUMN IF NOT EXISTS queries from the React Server Component render logic. Move these database schema migrations strictly into server.py's startup initialization routine to eliminate production deadlocks and connection pool starvation under high multi-tenant agent loads.
	•	QA-12 (Next.js Frontend — src/lib/db.ts): Virtualize Database SSL Connections: Strip out the hardcoded ssl: { rejectUnauthorized: false } rule from the connection pool setup. Bind it to an environment toggle: ssl: process.env.DATABASE_SSL === "true" ? { rejectUnauthorized: false } : false. This allows local, uncertified Postgres containers to run out of the box.
	•	NEW-05 (server.py): Add Map-Reduce Telemetry Tracking Columns: Expand your core SQL table generation scripts to store Map-Reduce tracking metrics, including raw input sizes, optimized output sizes, deduplication savings percentages, and specific workload profiles.
	•	QA-04 / QA-13 (server.py) — Build the Composite Cache Key Architecture: Redesign the semantic cache signature algorithm. Stop hashing only the content string of the single final user message. Change the routine to generate an MD5 fingerprint capturing the entire active conversation messages array, target model parameters, temperature, and strict schemas to eliminate cross-tenant cache collisions and context leaks.
	•	QA-08 (server.py) — Fix Concurrent Request Double-Billing Exploit: Modify the parallel request deduplication handler. If a waiting parallel request wakes up and pulls its completed response straight from the L1 memory cache, intercept the thread and bypass the cache fee ledger deduction.
	•	QA-17 (server.py) — Create the Physical Benchmarking Database Schema: Establish a physical table footprint inside the backend database routine to represent system evaluation and telemetry benchmarks.
	•	QA-20 (server.py) — Eliminate the Redundant Triple-Execution Bug: Fix the model fallback evaluation matrix. When a developer explicitly overrides the model parameter inside an incoming payload, ensure the framework skips the fallback loop entirely if the target recovery models are identical to the overridden model.
💻 Sprint 3: High-Fidelity Interactive Playground & Developer UX Overhaul
Primary Focus: Connect the frontend components directly to the backend data pipes to showcase a completely live, functional text extraction data engine.
📋 Task Breakdown & Code Modifications
	•	QA-18 (Next.js Frontend — src/app/cookbook/): Build the Live Multi-Workload Sandbox Cookbook: Completely tear out the hardcoded, static GovTech mockup layout from the dashboard. Replace it with a dynamic, data-driven drag-and-drop file upload playground. Allow users to select their target workload profile (Legal Contracts, Municipal Codes, or Video Transcripts), select or input custom extraction targets, chunk the file locally via the client, stream the tokens over the wire to POST /v1/swarm/map, and render the live verbatim result array alongside your "MAP_COMPLETE_INTERPRETATION_REQUIRED" architectural guidance note.
	•	NEW-06 (Next.js Frontend — src/app/dashboard/): Wire the Real-Time ROI Savings Ledger: Connect the dashboard's analytics charts to render the live value-based ledger math. Display a prominent ticker showing Net Enterprise Savings ($) running live based on your deduplication and caching metrics to visually prove your engine's financial efficiency.
	•	QA-14 (Next.js Frontend — src/app/dashboard/): Reveal Local Connection States Transparently: Strip out the obfuscation routines that hide database connection drops behind simulated mock metrics. If the database pool connection fails, render a prominent, clear alert banner notifying the user that the system is running in local read-only simulation mode, along with step-by-step configuration tips.
	•	QA-16 (Next.js Frontend — src/app/admin-console/): Populate the Real-Time Logging Matrices: Write the missing PostgreSQL database lookup queries inside the console route handler. Replace empty array placeholders with active database queries that pull real-time rows directly from api_logs, dlq_logs, and your newly constructed benchmarks tables straight into the admin analytics graphs and ledger layouts.
	•	QA-05 (server.py) — Repair the React Compiler Fallback Module: Wrap the sandboxed TypeScript compiler npx tsc subprocess execution block securely. If the terminal throws a compilation error due to missing type definitions or unconfigured local files, catch the non-zero exit code programmatically and route the payload cleanly into the lightweight self-healing fallback parser instead of returning a hard HTTP 400.
	•	QA-09 (Public Documentation — membrane-api.com/docs): Synchronize Documentation & Clear Amnesia Guides: Update the public integration manuals to explicitly highlight the default JIT local auto-provisioning framework, informing developers they can use any mock key string out of the box for testing without a pre-configured license key. Add prominent API recipe examples explaining how to pass the X-Membrane-Preserve-Context: true header when building multi-turn chat agents.
📥 Sprint 1 Handoff Packet Assets
To ensure the backend team can execute Sprint 1 without any ambiguity, hand over these explicit payload interface specs alongside the task list.
1. The Generalized POST /v1/swarm/map Input Spec
JSON
{
  "chunks": [
    "Text from page 1 or transcript block 1...",
    "Text from page 2 or transcript block 2..."
  ],
  "extraction_criteria": {
    "system_persona": "You are an expert data mining agent specializing in un-synthesized extraction.",
    "target_signals": ["Indemnification", "Liability Caps", "Zoning Setbacks", "Rhetorical Speech Hooks"],
    "enforce_verbatim": true
  }
}
2. The Structured /v1/swarm/map Output Spec
JSON
{
  "object": "swarm.extraction_matrix",
  "model": "membrane-engagement-layer",
  "task_id": "ext_matrix_99x7f2a4b",
  "extractions": [
    {
      "chunk_index": 0,
      "source_reference": "Chunk 1",
      "verbatim_text": "Extracted text string matching the target signal exactly...",
      "matched_signals": ["Liability Caps"]
    }
  ],
  "membrane_metadata": {
    "status": "MAP_COMPLETE_INTERPRETATION_REQUIRED",
    "total_raw_extractions_captured": 1,
    "deduplication_scrub_count": 0,
    "value_ledger": {
      "actual_cost_incurred": 0.0003,
      "gross_unoptimized_cost": 0.0045,
      "net_enterprise_savings": 0.0042
    },
    "architectural_guidance": {
      "alert": "HIGH_VOLUME_RAW_DATA_SIGNAL",
      "interpretation_note": "This payload represents an un-synthesized, high-fidelity verbatim map extraction array. To convert these raw needles into focused, contextual insights, Membrane strongly recommends passing this array downstream to a dedicated Interpretive Reduction Layer (e.g., Gemini 3.5 Flash) to filter out acceptable boilerplate."
    }
  }
}
3. Core Prompts Reference
Developers must refer to the provided SWARM_INSTRUCTIONS.md file from the contract scanner demo workspace for the underlying multi-agent zero-shot isolation protocols and prompt guidelines.

