# Internal Notes

This folder is for maintainer-facing engineering history, experiment trackers, and architectural notes that are useful context but not part of the public product docs.

Keep active test fixtures in `tests/`, active code in `membrane/` or `dashboard/`, and public setup instructions in the root `README.md`.
